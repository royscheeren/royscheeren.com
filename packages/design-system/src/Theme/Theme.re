open Css;

// Global Colours
let primaryColour = hex("2C5DF3");
let hoverColour = hex("1443D1");
let activeColour = hoverColour;
let disabledColour = hex("CECECE");
let white = hex("FFFFFF");
let gray = hex("CECECE")
let black = hex("121212");

// Text
let textPrimaryColour = black;
let textSecondaryColour = gray;
let textLinkColour = primaryColour;
let bodySize = rem(1.25);
let h1Size = rem(3.25);
let h2Size = rem(2.25);
